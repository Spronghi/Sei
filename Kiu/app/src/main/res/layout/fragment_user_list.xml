<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/fragment_user_list_coordinatorlayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- The ActionBar displayed at the top -->
    <include
        layout="@layout/toolbar"
        android:id="@+id/fragment_user_list_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="65dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:textColor="@color/accentColor"
            android:textStyle="bold"
            android:layout_margin="5dp"
            android:text="@string/open_post"/>

        <android.support.v7.widget.RecyclerView
            android:id="@+id/fragment_user_list_open_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            >
        </android.support.v7.widget.RecyclerView>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:textColor="@color/accentColor"
            android:textStyle="bold"
            android:layout_margin="5dp"
            android:text="@string/closed_post"/>

        <android.support.v7.widget.RecyclerView
            android:id="@+id/fragment_user_list_closed_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            >
        </android.support.v7.widget.RecyclerView>

    </LinearLayout>
</android.support.design.widget.CoordinatorLayout>